import MainBodyContainer from './MainBodyContainer';
import SideBar from './SideBar';

const Body = () => {
  return (
    <div>
      <SideBar />
      <MainBodyContainer />
    </div>
  );
};

export default Body;
