import VideoContainer from './VideoContainer';

const MainBodyContainer = () => {
  return (
    <div style={{ flex: 8 }}>
      <VideoContainer />
    </div>
  );
};

export default MainBodyContainer;
